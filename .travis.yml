language: ruby
cache: bundler
services: postgresql
bundler_args: --without production
rvm:
  - 2.3
  - jruby
  - 2.0.0-p247
script:
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec /test
